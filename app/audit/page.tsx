import { QuizForm } from '@/components/quiz-form';

export const metadata = {
  title: 'Sales Audit - Vioneer',
  description: 'Calculate your lost revenue with our quick sales audit.',
};

export default function AuditPage() {
  return <QuizForm />;
}
